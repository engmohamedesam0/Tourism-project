@model Tourist_Project_MVC.Models.Mission

@{
    ViewData["Title"] = @Localizer["Mission_Page_Title_Details"].Value;
}

@if (User.Identity.IsAuthenticated)
{
    <div class="container mt-4" style="max-width: 800px;">
        <div class="card shadow-sm border-0 rounded-4 overflow-hidden">

            <div class="card-header py-3"
                 style="background: linear-gradient(135deg, var(--egy-dark), #C8832A);">
                <div class="d-flex justify-content-between align-items-center">
                    <h4 class="text-white mb-0">
                        <i class="bi bi-trophy-fill me-2"></i> @Model.Title
                    </h4>
                    <span class="badge bg-warning text-dark fs-6">
                        @Model.MissionType
                    </span>
                </div>
            </div>

            <div class="card-body p-4">

                <div class="row text-center mb-4">
                    <div class="col-3">
                        <div class="stat-mini-card">
                            <i class="bi bi-star-fill text-warning"></i>
                            <div class="stat-mini-number">@Model.PointsReward</div>
                            <div class="stat-mini-label">@Localizer["Mission_Label_Points"].Value</div>
                        </div>
                    </div>
                    <div class="col-3">
                        <div class="stat-mini-card">
                            <i class="bi bi-geo-alt-fill text-danger"></i>
                            <div class="stat-mini-number">@Model.Destination?.Name</div>
                            <div class="stat-mini-label">@Localizer["Mission_Label_Destination"].Value</div>
                        </div>
                    </div>
                    <div class="col-3">
                        <div class="stat-mini-card">
                            <i class="bi bi-hourglass-split text-primary"></i>
                            <div class="stat-mini-number">
                                @(Model.UserMissions?.Count(um => um.Status == "In Progress") ?? 0)
                            </div>
                            <div class="stat-mini-label">@Localizer["Mission_Status_InProgress"].Value</div>
                        </div>
                    </div>
                    <div class="col-3">
                        <div class="stat-mini-card">
                            <i class="bi bi-check-circle-fill text-success"></i>
                            <div class="stat-mini-number">
                                @(Model.UserMissions?.Count(um => um.Status == "Completed") ?? 0)
                            </div>
                            <div class="stat-mini-label">@Localizer["Mission_Status_Completed"].Value</div>
                        </div>
                    </div>
                </div>

                <hr />

                <dl class="row">
                    <dt class="col-sm-3 text-muted">@Localizer["Mission_Label_Type"].Value</dt>
                    <dd class="col-sm-9">
                        <span class="badge-category">@Model.MissionType</span>
                    </dd>

                    <dt class="col-sm-3 text-muted">@Localizer["Mission_Label_Description"].Value</dt>
                    <dd class="col-sm-9">@Model.Description</dd>
                </dl>
            </div>

            <div class="card-footer bg-white d-flex gap-2 justify-content-end">
                @if (User.IsInRole("Admin"))
                {
                    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">
                    <i class="bi bi-pencil-fill me-1"></i> @Localizer["Mission_Button_Edit"].Value
                </a>
                }
                <a asp-action="Index" class="btn-back">
                    <i class="bi bi-arrow-left me-1"></i> @Localizer["Mission_Button_Back"].Value
                </a>
            </div>
        </div>
    </div>
}

@section Scripts {
    <style>
        .stat-mini-card {
            background: #FDF6EC;
            border: 1px solid #E8A045;
            border-radius: 12px;
            padding: 15px 10px;
        }

        .stat-mini-number {
            font-family: 'Cinzel',sans-serif;
            font-size: 1rem;
            font-weight: 700;
            color: var(--egy-dark);
            margin: 6px 0 2px;
        }

        .stat-mini-label {
            font-size: 0.75rem;
            color: #999;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .badge-category {
            background-color: #FDF6EC;
            color: var(--egy-primary);
            border: 1px solid var(--egy-accent);
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 0.78rem;
            font-weight: 600;
        }
    </style>
}
