@model Tourist_Project_MVC.View_Model.AdminSupportIndexVM

@{
    ViewData["Title"] = @Localizer["AdminSupport_SupportInbox"].Value;
    ViewData["IsHomePage"] = false;
}

<div class="container my-4" style="max-width: 1200px;">

    <h1 class="page-title mb-4">
        <i class="bi bi-headset me-2"></i> @Localizer["AdminSupport_SupportInbox"].Value
    </h1>

    @await Html.PartialAsync("_StatBoxRow", (List<Tourist_Project_MVC.View_Model.StatBoxItem>)ViewBag.StatBoxes)

    <!-- Filter Bar -->
    <div class="filter-bar mb-3">
        <form method="get" asp-controller="AdminSupport" asp-action="Index">
            <div class="row g-2 align-items-end">
                <div class="col-md-3">
                    <label class="form-label small text-muted mb-1">@Localizer["AdminSupport_Search"].Value</label>
                    <input type="text" name="search" class="form-control" value="@Model.Search" placeholder="@Localizer["AdminSupport_SubjectOrDescription"].Value" />
                </div>
                <div class="col-md-2">
                    <label class="form-label small text-muted mb-1">@Localizer["AdminSupport_Status"].Value</label>
                    <select name="status" class="form-select">
                        <option value="">@Localizer["AdminSupport_AllStatuses"].Value</option>
                        @foreach (var st in Model.Statuses)
                        {
                            if (Model.StatusFilter == st)
                            {
                                <option value="@st" selected>@st</option>
                            }
                            else
                            {
                                <option value="@st">@st</option>
                            }
                        }
                    </select>
                </div>
                <div class="col-md-2">
                    <label class="form-label small text-muted mb-1">@Localizer["AdminSupport_Category"].Value</label>
                    <select name="category" class="form-select">
                        <option value="">@Localizer["AdminSupport_AllCategories"].Value</option>
                        @foreach (var c in Model.Categories)
                        {
                            if (Model.CategoryFilter == c)
                            {
                                <option value="@c" selected>@c</option>
                            }
                            else
                            {
                                <option value="@c">@c</option>
                            }
                        }
                    </select>
                </div>
                <div class="col-md-2">
                    <label class="form-label small text-muted mb-1">@Localizer["AdminSupport_Submitter"].Value</label>
                    <select name="submitterType" class="form-select">
                        <option value="">@Localizer["AdminSupport_All"].Value</option>
                        @foreach (var st in Model.SubmitterTypes)
                        {
                            if (Model.SubmitterTypeFilter == st)
                            {
                                <option value="@st" selected>@st</option>
                            }
                            else
                            {
                                <option value="@st">@st</option>
                            }
                        }
                    </select>
                </div>
                <div class="col-md-2 d-grid">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-funnel-fill me-1"></i> @Localizer["AdminSupport_Filter"].Value
                    </button>
                </div>
                <div class="col-md-1 d-grid">
                    <a asp-action="Index" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-counterclockwise"></i>
                    </a>
                </div>
            </div>
        </form>
    </div>

    <div class="card border-0 shadow-sm rounded-4">
        <div class="card-body">
            @if (!Model.Tickets.Any())
            {
                <div class="text-center py-5 text-muted">
                    <i class="bi bi-inbox" style="font-size:2.5rem;"></i>
                    <p class="mb-0 mt-2">@Localizer["AdminSupport_NoTicketsFound"].Value</p>
                </div>
            }
            else
            {
                <div class="table-responsive">
                    <table class="table table-hover align-middle destinations-table">
                        <thead>
                                     <tr>
                                         <th>@Localizer["AdminSupport_Type"].Value</th>
                                         <th>@Localizer["AdminSupport_Submitter"].Value</th>
                                         <th>@Localizer["AdminSupport_RoutedTo"].Value</th>
                                         <th>@Localizer["AdminSupport_Category"].Value</th>
                                         <th>@Localizer["AdminSupport_Subject"].Value</th>
                                         <th>@Localizer["AdminSupport_Status"].Value</th>
                                         <th>@Localizer["AdminSupport_Submitted"].Value</th>
                                         <th></th>
                                     </tr>
                        </thead>
                        <tbody>
                            @foreach (var row in Model.Tickets)
                            {
                                <tr>
                                    <td>
                                        <span class="badge bg-@(row.SubmitterType == "Tourist -> Sponsor" ? "primary" : row.SubmitterType == "Tourist" ? "info" : "secondary")">
                                            <i class="bi bi-@(row.SubmitterType == "Tourist -> Sponsor" ? "arrow-right" : row.SubmitterType == "Tourist" ? "person" : "building") me-1"></i>@row.SubmitterType
                                        </span>
                                    </td>
                                    <td class="fw-semibold">@row.SubmitterName</td>
                                    <td class="fw-semibold text-primary">@(row.RoutedSponsorName ?? "-")</td>
                                    <td><span class="badge-category">@row.Ticket.Category</span></td>
                                    <td>@row.Ticket.Subject</td>
                                    <td>
                                        <span class="badge bg-@(row.Ticket.Status == "Open" ? "warning" : row.Ticket.Status == "Resolved" ? "success" : "info")">@row.Ticket.Status</span>
                                    </td>
                                    <td class="small text-muted">@row.Ticket.CreatedDate.ToString("yyyy-MM-dd")</td>
                                    <td class="text-end">
                                        <a asp-action="Details" asp-route-id="@row.Ticket.Id" class="btn btn-sm btn-outline-primary">
                                            <i class="bi bi-box-arrow-in-right me-1"></i> @Localizer["AdminSupport_Open"].Value
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </div>
</div>
